
    # PostgreSQL Config
POSTGRES_USER=user
POSTGRES_PASSWORD=pass
POSTGRES_DB=transcendence_db
POSTGRES_HOST=ft_database
POSTGRES_DB=db_member
POSTGRES_PORT=5432

    # Postgres exporter
DATA_SOURCE_NAME=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@ft_database:5432/${POSTGRES_DB}?sslmode=disable

    # Redis Config
REDIS_ADDR=redis:6379

    # Grafana Config
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=42admin
GF_USERS_ALLOW_SIGN_UP=false
GF_SERVER_ROOT_URL=https://localhost:3002
GF_SERVER_HTTP_PORT=3002
GF_SERVER_PROTOCOL=https
GF_SERVER_CERT_FILE=/etc/grafana/ssl/certificate.crt
GF_SERVER_CERT_KEY=/etc/grafana/ssl/certificate.key

    # Prometheus Config
PROMETHEUS_PASSWORD=prometheus_password

    # Email Alerting Config
SMTP_TO=ok.lyrics.emirate@gmail.com
SMTP_FROM=oakphoenix1@gmail.com
SMTP_SMARTHOST=smtp.gmail.com:587
SMTP_AUTH_USERNAME=oakphoenix1@gmail.com
SMTP_AUTH_IDENTITY=oakphoenix1@gmail.com
SMTP_AUTH_PASSWORD=pzcepuicizpcvzav
